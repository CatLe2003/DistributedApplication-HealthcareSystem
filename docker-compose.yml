version: "3.9"

services:
  # -------------------------
  # Patient Service + DB
  # -------------------------
  patientservice:
    build: ./PatientService
    container_name: patientservice
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    depends_on:
      - db
    networks:
      - patient_network
      - gateway_network # connects also to gateway
    environment:
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: patient_db
      DB_USERNAME: root
      DB_PASSWORD: root

  db:
    image: mysql:8.0
    container_name: patient_db
    restart: always
    environment:
      MYSQL_DATABASE: patient_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - patient_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: patient_phpmyadmin
    restart: always
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: root
    depends_on:
      - db
    networks:
      - patient_network

  # -------------------------
  # API Gateway
  # -------------------------
  api_gateway:
    build: ./ApiGateway
    container_name: api_gateway
    restart: always
    ports:
      - "9000:80" # external API Gateway entry
    networks:
      - gateway_network
    volumes:
    - ./ApiGateway/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - patientservice

  # -------------------------
  # Frontend
  # -------------------------
  frontend:
    build:
      context: ./frontend-laravel
    container_name: frontend
    restart: always
    ports:
      - "8082:8000"
    networks:
      - gateway_network
    depends_on:
      - api_gateway
    volumes:
      - ./frontend-laravel:/var/www/html # sync code with container
      - ./frontend-laravel/vendor:/var/www/html/vendor  # Persist composer dependencies
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    command: >
      sh -c "composer install --ignore-platform-reqs &&
             php artisan serve --host=0.0.0.0 --port=8000"

  # -------------------------
  # Employee Service 
  # -------------------------
  employeeservice:
    build: ./EmployeeService
    container_name: employee_service
    ports:
      - "8003:8000"
    volumes:
      - ./EmployeeService:/var/www/html
      - ./EmployeeService/vendor:/var/www/html/vendor
    networks:
      - employee_network
      - gateway_network
    environment:
      - APP_NAME=Laravel
      - APP_ENV=local
      - APP_KEY=base64:JH+cB2XMtncxBVhjEccrERFgAlVtsgrolGLU+1eoGTQ=
      - APP_DEBUG=true
      - APP_URL=http://localhost:8003
      - DB_CONNECTION=pgsql
      - DB_URL=postgresql://postgres:songcat_employeedb@db.krmbzznitpedivsepeyw.supabase.co:5432/postgres
      - DB_PASSWORD=songcat_employeedb
      - SESSION_DRIVER=database
      - CACHE_STORE=database
      - QUEUE_CONNECTION=database
    command: >
      sh -c "composer install --ignore-platform-reqs &&
             php artisan serve --host=0.0.0.0 --port=8000"


  # -------------------------
  # Authentication Service
  # -------------------------
  authentication_service:
    build: ./AuthenticationService
    container_name: authentication_service
    ports:
      - "8002:8000"
    networks:
      - auth_network
      - gateway_network
    environment:
      - APP_NAME=Laravel
      - APP_ENV=local
      - APP_KEY=base64:JH+cB2XMtncxBVhjEccrERFgAlVtsgrolGLU+1eoGTQ=
      - APP_DEBUG=true
      - APP_URL=http://localhost:8002
      - DB_CONNECTION=pgsql
      - DB_URL=postgresql://postgres.zrhqaqklduaafeoqjcoj:songcat_authenticationdb@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres
      - DB_PASSWORD=songcat_authenticationdb
      - SESSION_DRIVER=database
      - SESSION_LIFETIME=120
      - CACHE_STORE=database
      - QUEUE_CONNECTION=database
      - API_GATEWAY_URL=http://api_gateway
      - JWT_SECRET=songcat_authenticationdb
    command: >
      sh -c "composer install --ignore-platform-reqs &&
             php artisan migrate --force &&
             php artisan serve --host=0.0.0.0 --port=8000"

  # -------------------------
# Volumes
# -------------------------
volumes:
  mysql_data:
# Networks
# -------------------------
networks:
  patient_network:
    driver: bridge
  gateway_network:
    driver: bridge
  employee_network:
    driver: bridge
  auth_network:
    driver: bridge
